<% if @consult_request.persisted? %>
  alert("<%= t('text.consult_request_received') %>");
  location.reload();
<% else %>
  $('#consult_form_block').html("<%= escape_javascript(render('form')) %>");
<% end %>